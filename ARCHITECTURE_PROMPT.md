# Architecture Prompt

---

# ✅ 🔥 Golden Prompt Template

Just follow this structure:

```text
Create a one-page architecture diagram in hand-drawn whiteboard style (like Excalidraw / GoodNotes).

Include:
- Sectioned layout (like example I shared)
- Color-coded flows (lexical vs semantic)
- Boxes with rounded corners
- Handwritten-style annotations and arrows

System:
<PASTE YOUR CODE OR DESCRIPTION>

Sections to include:
1. Config / Environment
2. Ingestion Flow
3. Index Structure
4. Search Flow (BM25 + Vector)
5. Output

Style:
- Handwritten fonts
- Colors: teal, pink, purple
- Dotted borders for sections
- Arrows with labels
- Clean but informal whiteboard look

Make it visually similar to a BM25/TF-IDF explainer diagram.
```

---

# 🎯 If you want EVEN better results

Add this:

```text
Make it look like:
- Excalidraw diagram
- YouTube ML whiteboard tutorial style
- Not like a corporate architecture diagram
```

---

# 🧠 Why this works

You’re telling me:

* **WHAT** → architecture content
* **HOW** → sections/layout
* **STYLE** → visual language
* **REFERENCE** → exact aesthetic

👉 That’s what made your last diagram look good

---

# 🚀 Pro-level prompts (based on your use case)

Since you’re working with:

* OpenSearch
* Embeddings
* Hybrid search

Use this variant:

```text
Create a one-page hybrid search architecture diagram in hand-drawn whiteboard style.

Highlight:
- BM25 (inverted index path)
- Vector search (HNSW path)
- Embedding generation
- Query flow (both paths side by side)

Clearly show:
- Same index used by both search types
- Query splitting into lexical + semantic paths
- Final result aggregation

Style: Excalidraw / ML whiteboard explainer
```

---

# ⚡ Shortcut version (what you can type quickly)

```text
Make a one-page architecture diagram in hand-drawn Excalidraw style with:
- ingestion flow
- index structure
- BM25 + vector search flows
- color-coded paths
based on this code:
<code>
```

---

# 👍 Bonus tip

If you already have an image (like you did), say:

```text
Make it visually identical to this style:
<attach image>
```

👉 That’s what got you the best result

---

If you want next:
I can create a **reusable prompt cheat sheet PDF** or a **VS Code snippet template** so you can generate diagrams instantly 👍
